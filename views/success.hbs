<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10">
            <div class="text-center mb-5">
                <div class="success-icon mb-4">
                    <i class="fa fa-check-circle fa-5x text-success"></i>
                </div>
                <h1 class="display-4 fw-bold text-success mb-3">Welcome!</h1>
                <p class="lead">Thank you for subscribing to the {{plan}} plan!</p>
                <p class="text-muted">We're processing your subscription and you'll receive an email confirmation shortly.</p>
            </div>

            <div class="text-center mt-5">
                <a href="/dashboard" class="btn btn-success btn-lg px-5">
                    Go to Dashboard
                </a>
            </div>

            <div class="text-center mt-4">
                <p class="text-muted">
                    Need help? Contact us at 
                    <a href="mailto:support@tryclosed.com">support@tryclosed.com</a>
                </p>
            </div>
        </div>
    </div>
</div>

<style>
.success-icon {
    animation: checkmark 0.8s ease-in-out;
}

@keyframes checkmark {
    0% {
        transform: scale(0);
        opacity: 0;
    }
    50% {
        transform: scale(1.2);
    }
    100% {
        transform: scale(1);
        opacity: 1;
    }
}

.step-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
}

.step-icon {
    flex-shrink: 0;
    width: 30px;
    height: 30px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
}

.step-content h6 {
    margin-bottom: 0.5rem;
    font-weight: 600;
}
</style> 